import { AppHighlightBig } from './app-highlight-big';

describe('AppHighlightBig', () => {
  it('should create an instance', () => {
    const directive = new AppHighlightBig();
    expect(directive).toBeTruthy();
  });
});
